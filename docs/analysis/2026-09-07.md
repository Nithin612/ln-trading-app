# Daily Trading Analysis — 2026-09-07 (Monday)

_Generated 2026-09-07 22:24 IST · paper mode · user **Nithin** · capital ₹100,000 · risk 2.00%/trade · daily-loss cap ₹3,000 · exit governor: **ON (₹ profit ladder)**_

> Read-only, reproducible. Money net of Zerodha charges where realised; open marks are the day's last 1m close. Raw AlertBell firings are ephemeral (Redis stream) — the alert recap is reconstructed from the durable signal + signal-outcome record.

> **Profit ladder (live):** breakeven at +₹2,000 profit · seal (peak − ₹1,000) once peak ≥ ₹3,000 · ATR room ×2.0.

> ## ⚠️ FEED STALENESS ALARM (live — as of report generation)
>
> One or more EOD feeds are behind the trading calendar — the 07-02→07-17 silent-outage failure mode. Check ingestion (worker + Kite token) before trusting today's numbers; the self-healer catches ≤21 days.
>
> - **Equity EOD** (`ohlcv_1d`): latest 2026-09-04, expected ≥ 2026-09-07 — **1** trading day(s) behind
> - **F&O bhavcopy** (`fo_bhavcopy`): latest —, expected ≥ 2026-09-07 — **NO DATA AT ALL**
> - **FII/DII flows** (`fii_dii_daily`): latest —, expected ≥ 2026-09-07 — **NO DATA AT ALL**

> ## ⚠️ WORKER LIVENESS ALARM
>
> A role has not checked in. Beat-driven work is NOT running, and the failures that follow are silent by nature — nothing raises when a task never starts.
>
> - **live_worker** — last heartbeat never seen. Covers: the live tick path — provisional scoring and depth capture


> ## ⛔ CAS WINDOW MISSED — UNRECOVERABLE
>
> 2026-09-07 was a trading day, its 15:15–15:33 IST window has closed, and **zero rows were captured**. The closing auction cannot be replayed, so this day is permanently absent from the CAS study. Check that `make worker` is up before the next session.

> ## ⚠️ TICK-MODE DEGRADATION (A25)
>
> The live feed delivered ticks the depth path cannot use, so `depth:{stock_id}` went stale for those stocks and their paper fills silently fell back to the flat `paper_slippage_bps` floor — **fills cheaper than reality, on the book expectancy is judged with**.
>
> - **Full-mode ticks with no usable book:** **113,575** — one-sided/crossed/empty depth. Normal around the open; a large count during the session is not.

## 1. Scorecard

- **Entries today:** 0 · **Exits today:** 0 · **Open at day end:** 0
- **Realised today (net):** +₹0  ·  **Open book mark-to-market (gross, EoD):** +₹0
- **vs buy-and-hold:** not assessable (needs a paper-clock start, a capital figure, and NIFTY 50 bars at both ends of the window)
- **Trials attempted (observed): 15** vs **20 assumed** by the deflated-Sharpe bar — the bar is currently **stricter** than our search by 5
  - 2 promoted-then-reverted · 3 decided against · 8 still in shadow · 2 active
  - ⚠ **observed is a LOWER BOUND**: one entry is one HYPOTHESIS, but most were tried at several thresholds (`sl_atr`'s k, chase's 0.33R, circuit's 1.5%, liquidity's floor) and each variant is its own trial. The register does not record variants yet, so "observed < assumed, therefore the bar is conservative" does NOT follow.
  - ⚠ this is a REPORT, not a change: `DEFAULT_TRIALS` is untouched, because raising the assumed N makes the bar harder for every candidate and that is a decision for a person, not a side effect.
  - reverted: **Regime gate — skip transitional ADX 20–25** · **R:R ≥ 1 floor** — kept visible on purpose; a programme that forgets its failures re-runs them
- **Profit given back** (peak → EoD on open positions that faded): **+₹0**
- **Profit sealed right now** (Σ locked-in floors across open positions): **+₹0** — the ₹ guaranteed if every stop holds
- **Open portfolio heat** (Σ risk-at-fill on open positions): ₹0 (0.0% of capital)

## 2. What you traded vs the plan

| Stock | Dir/Class | Conf | Fill | Sig entry | Chase | Risk vs 2% | Risk ₹ (%cap) | RR@fill / plan | SL now | Status |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---|

_Chase = fill vs the signal's entry, in R (>0.33R ⚠️ = past the "don't-chase" ceiling AlertBell shows). Risk vs 2% = the rupee risk this position actually carries ÷ your intended per-trade budget (capital × 2%); >1 means you're risking more than your setting._

## 3. Per-trade tape read — entry/exit timing & counterfactual

_No entries opened on this day._

## 4. Engine performance — predicted vs happened


## 5. Risk analysis

- No materially oversized entries today.
- **Portfolio heat:** open positions risk ₹0 (0.0% of the ₹100,000 LIVE capital) if every stop is hit. Against the declared paper SAMPLING scale of ₹500,000 that is **0.0%** — the sampler is intentionally wide (≈5 entries/day × ~5-day holds ⇒ ~25 concurrent positions) to accrue entry/exit evidence fast. Neither number alone is the whole picture: the live figure says what a ₹1L account would be carrying, the sampling figure says whether the experiment itself is sanely sized.

## 6. Takeaways

- **Action items:** see `docs/analysis/FIX_PLAN.md` (P1 chase→resize guard, P2 earlier profit-lock arming + tape retune, P3 trade-from-AlertBell).

## 7. F&O option-selling engine

> Suggestions only — there is no F&O order path (live trading is Phase 7). This section exists to review two open calibration decisions (`phase-04-fo-suggestions.md` §9.3 / §9.7); **a dark day is only meaningful with its reason.**

| Underlying | Verdict | Expiry (DTE) | Why |
|---|---|---|---|
| BANKNIFTY | — no data | — | no F&O bhavcopy recorded at or before this day |
| FINNIFTY | — no data | — | no F&O bhavcopy recorded at or before this day |
| NIFTY | — no data | — | no F&O bhavcopy recorded at or before this day |

- No policy or data-freshness flags today.

## 8. Intraday shadow layer

_No profiles are running in shadow._

## 9. Fill realism — spread-aware slippage (6.8.2)

_No paper fills carrying fill-model telemetry on this day._ (Orders placed before 6.8.2 have none; a day with no trades has none.)

